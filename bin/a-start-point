#!/usr/bin/env node  

const port = require('../server/b-config-from-env').port || 3000;  //port number might be taken from environmetal constans with config or by default 3000 
const app = require('../server/d-server-module');
const server = app.listen(port, () => {});